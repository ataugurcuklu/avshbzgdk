---
import { Phone, Mail, MapPin } from "lucide-preact";
---

<section class="py-12 md:py-20 bg-gray-100">
  <div class="container mx-auto px-4 md:px-6">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 md:gap-12">
      <div class="bg-white shadow-lg rounded-lg p-4 md:p-8">
        <h3 class="text-xl md:text-2xl font-semibold text-law-primary mb-4">
          İletişim Bilgilerimiz
        </h3>
        <div class="space-y-3 md:space-y-4">
          <div class="flex items-start space-x-3 md:space-x-4">
            <Phone
              class="w-5 h-5 md:w-6 md:h-6 text-law-primary mt-1 flex-shrink-0"
            />
            <div class="text-sm md:text-base">
              <div>0546 567 36 06</div>
              <div>0541 915 65 87</div>
            </div>
          </div>
          <div class="flex items-start space-x-3 md:space-x-4">
            <Mail
              class="w-5 h-5 md:w-6 md:h-6 text-law-primary mt-1 flex-shrink-0"
            />
            <span class="text-sm md:text-base break-all"
              >sahbazgedikavukatlikburosu@gmail.com</span
            >
          </div>
          <div class="flex items-start space-x-3 md:space-x-4">
            <MapPin
              class="w-5 h-5 md:w-6 md:h-6 text-law-primary mt-1 flex-shrink-0"
            />
            <span class="text-sm md:text-base leading-relaxed">
              Mehmet Akif Ersoy Mahallesi 274. Sokak 1/B Wings Plaza No: 78
              Yenimahalle/Ankara
            </span>
          </div>
        </div>

        <h3
          class="text-xl md:text-2xl font-semibold text-law-primary mt-6 mb-4"
        >
          Mesaj Gönderin
        </h3>
        <form class="space-y-3 md:space-y-4" id="contactForm">
          <div>
            <input
              type="text"
              name="name"
              placeholder="İsim"
              required
              class="w-full px-3 md:px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm md:text-base"
            />
          </div>
          <div>
            <input
              type="email"
              name="email"
              placeholder="E-Posta"
              required
              class="w-full px-3 md:px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm md:text-base"
            />
          </div>
          <div>
            <input
              type="tel"
              name="phone"
              placeholder="Telefon"
              class="w-full px-3 md:px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm md:text-base"
            />
          </div>
          <div>
            <textarea
              name="message"
              placeholder="Mesajınız"
              rows="3"
              required
              class="w-full px-3 md:px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm md:text-base resize-none md:resize-y"
            ></textarea>
          </div>

          <div
            class="bg-blue-50 border border-blue-200 rounded-lg p-3 text-xs md:text-sm text-blue-800"
          >
            <p class="mb-2">
              <strong>Gizlilik Bildirimi:</strong> Bu formu doldurarak, size daha
              iyi hizmet verebilmek için kişisel bilgilerinizin (isim, e-posta, telefon
              ve mesaj içeriği) saklanmasını kabul etmiş olursunuz.
            </p>
            <p>
              Verileriniz güvenli bir şekilde korunur ve sadece size geri dönüş
              yapmak için kullanılır. Detaylı bilgi için <a
                href="/sss"
                class="underline hover:text-blue-900">SSS sayfamızı</a
              > inceleyebilirsiniz.
            </p>
          </div>

          <button
            type="submit"
            id="submitBtn"
            class="w-full bg-law-secondary text-white py-2 px-4 rounded-md hover:bg-law-primary transition duration-300 text-sm md:text-base"
            >Gönder</button
          >
        </form>
        <div id="messageStatus" class="mt-4 hidden"></div>
      </div>

      <div class="bg-white shadow-lg rounded-lg p-2 md:p-4">
        <div class="w-full rounded-lg overflow-hidden">
          <iframe
            src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3058.05963086958!2d32.770192077430366!3d39.96241947151624!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x14d3490dc0cf28a7%3A0xe800e728f215aa8a!2sWings%20Ankara!5e0!3m2!1str!2str!4v1758982525815!5m2!1str!2str"
            style="border:0;"
            width="100%"
            height="300"
            class="md:h-[500px] lg:h-[720px]"
            allowfullscreen=""
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"></iframe>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const form = document.getElementById("contactForm") as HTMLFormElement;
    const submitBtn = document.getElementById("submitBtn") as HTMLButtonElement;
    const messageStatus = document.getElementById(
      "messageStatus"
    ) as HTMLDivElement;

    form.addEventListener("submit", async function (e) {
      e.preventDefault();

      submitBtn.disabled = true;
      submitBtn.textContent = "Gönderiliyor...";

      messageStatus.classList.add("hidden");

      try {
        const formData = new FormData(form);

        const response = await fetch("/api/contact", {
          method: "POST",
          body: formData,
        });

        const result = await response.json();

        if (response.ok) {
          messageStatus.textContent =
            "Mesajınız başarıyla gönderildi! Size en kısa sürede dönüş yapacağız.";
          messageStatus.className =
            "mt-4 p-3 bg-green-100 text-green-700 rounded-md text-sm md:text-base";
          form.reset();
        } else {
          messageStatus.textContent =
            result.error || "Mesaj gönderilirken bir hata oluştu.";
          messageStatus.className =
            "mt-4 p-3 bg-red-100 text-red-700 rounded-md text-sm md:text-base";
        }
      } catch (error) {
        messageStatus.textContent = "Bağlantı hatası. Lütfen tekrar deneyin.";
        messageStatus.className =
          "mt-4 p-3 bg-red-100 text-red-700 rounded-md text-sm md:text-base";
      }

      messageStatus.classList.remove("hidden");

      submitBtn.disabled = false;
      submitBtn.textContent = "Gönder";
    });
  });
</script>
