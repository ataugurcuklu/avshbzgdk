---
import { Phone, Mail, MapPin } from "lucide-preact";
---

<section class="py-20 bg-gray-100">
  <div class="container mx-auto px-6">
    <div class="grid md:grid-cols-2 gap-12">
      <div class="bg-white shadow-lg rounded-lg p-4">
        <div class="w-full rounded-lg overflow-hidden justify-center items-center">
          <iframe
            src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d58228.33833486682!2d32.74861197058513!3d39.91342267205548!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x14d347d520732db1%3A0xbdc57b0c0842b8d!2sAnkara!5e0!3m2!1str!2str!4v1739703870439!5m2!1str!2str"
            style="border:0;"
            width="100%"
            height="720"
            allowfullscreen=""
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"></iframe>
        </div>
      </div>
      <div class="bg-white shadow-lg rounded-lg p-8">
        <h3 class="text-2xl font-semibold text-law-primary mb-4">
          İletişim Bilgilerimiz
        </h3>
        <div class="space-y-4">
          <div class="flex items-center space-x-4">
            <Phone class="w-6 h-6 text-law-primary" />
            <span>(123) 456-7890</span>
          </div>
          <div class="flex items-center space-x-4">
            <Mail class="w-6 h-6 text-law-primary" />
            <span>mail@mail.com</span>
          </div>
          <div class="flex items-center space-x-4">
            <MapPin class="w-6 h-6 text-law-primary" />
            <span>123 Legal Street, Lawville, ST 12345</span>
          </div>
        </div>
        <h3 class="text-2xl font-semibold text-law-primary mt-6 mb-4">
          Mesaj Gönderin
        </h3>
        <form class="space-y-4" id="contactForm">
          <div>
            <input
              type="text"
              name="name"
              placeholder="İsim"
              required
              class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
            />
          </div>
          <div>
            <input
              type="email"
              name="email"
              placeholder="E-Posta"
              required
              class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
            />
          </div>
          <div>
            <input
              type="tel"
              name="phone"
              placeholder="Telefon"
              class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
            />
          </div>
          <div>
            <textarea
              name="message"
              placeholder="Mesajınız"
              rows="4"
              required
              class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
            ></textarea>
          </div>
          
          <div class="bg-blue-50 border border-blue-200 rounded-lg p-3 text-sm text-blue-800">
            <p class="mb-1">
              <strong>Gizlilik Bildirimi:</strong> Bu formu doldurarak, size daha iyi hizmet verebilmek için 
              kişisel bilgilerinizin (isim, e-posta, telefon ve mesaj içeriği) saklanmasını kabul etmiş olursunuz.
            </p>
            <p>
              Verileriniz güvenli bir şekilde korunur ve sadece size geri dönüş yapmak için kullanılır. 
              Detaylı bilgi için <a href="/sss" class="underline hover:text-blue-900">SSS sayfamızı</a> inceleyebilirsiniz.
            </p>
          </div>
          
          <button
            type="submit"
            id="submitBtn"
            class="w-full bg-law-secondary text-white py-2 px-4 rounded-md hover:bg-law-primary transition duration-300"
            >Gönder</button
          >
        </form>
        <div id="messageStatus" class="mt-4 hidden"></div>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const form = document.getElementById('contactForm') as HTMLFormElement;
    const submitBtn = document.getElementById('submitBtn') as HTMLButtonElement;
    const messageStatus = document.getElementById('messageStatus') as HTMLDivElement;

    form.addEventListener('submit', async function(e) {
      e.preventDefault();
      
      // Disable submit button
      submitBtn.disabled = true;
      submitBtn.textContent = 'Gönderiliyor...';
      
      // Hide previous messages
      messageStatus.classList.add('hidden');
      
      try {
        const formData = new FormData(form);
        
        const response = await fetch('/admin/api/contact', {
          method: 'POST',
          body: formData
        });
        
        const result = await response.json();
        
        if (response.ok) {
          messageStatus.textContent = 'Mesajınız başarıyla gönderildi! Size en kısa sürede dönüş yapacağız.';
          messageStatus.className = 'mt-4 p-3 bg-green-100 text-green-700 rounded-md';
          form.reset();
        } else {
          messageStatus.textContent = result.error || 'Mesaj gönderilirken bir hata oluştu.';
          messageStatus.className = 'mt-4 p-3 bg-red-100 text-red-700 rounded-md';
        }
      } catch (error) {
        messageStatus.textContent = 'Bağlantı hatası. Lütfen tekrar deneyin.';
        messageStatus.className = 'mt-4 p-3 bg-red-100 text-red-700 rounded-md';
      }
      
      // Show message
      messageStatus.classList.remove('hidden');
      
      // Re-enable submit button
      submitBtn.disabled = false;
      submitBtn.textContent = 'Gönder';
    });
  });
</script>